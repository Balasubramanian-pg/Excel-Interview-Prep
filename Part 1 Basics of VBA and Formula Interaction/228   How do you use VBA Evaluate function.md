### 228. **How do you use VBA Evaluate function?**

```
Sub UseEvaluate()
    Dim result As Variant

    ' Evaluate simple expressions
    result = Evaluate("5 + 3 * 2")  ' Returns 11
    Debug.Print result

    ' Evaluate Excel formulas
    result = Evaluate("=SUM(A1:A10)")
    Debug.Print result

    ' Using bracket notation (shorthand)
    result = [SUM(A1:A10)]
    Debug.Print result

    ' Complex formulas
    result = [SUMPRODUCT((A1:A100=""West"")*(B1:B100>1000))]

    ' Evaluate array formulas
    result = Evaluate("=TRANSPOSE(A1:A5)")
    ' Returns array

    ' Error handling
    On Error Resume Next
    result = Evaluate("=InvalidFormula()")
    If Err.Number <> 0 Then
        MsgBox "Formula error: " & Err.Description
        Err.Clear
    End If
    On Error GoTo 0
End Sub

```

**Practical Evaluate Uses:**

```
Sub PracticalEvaluate()
    ' Quick calculations without helper cells
    Dim maxValue As Double
    maxValue = [MAX(A:A)]

    ' Dynamic range evaluation
    Dim lastRow As Long
    lastRow = Cells(Rows.Count, 1).End(xlUp).Row

    Dim sumValue As Double
    sumValue = Evaluate("SUM(A1:A" & lastRow & ")")

    ' Conditional evaluation
    Dim conditional As Variant
    conditional = Evaluate("=SUMIF(A:A,"">100"",B:B)")

    ' Check if value exists
    Dim exists As Boolean
    exists = Evaluate("COUNTIF(A:A,""SearchValue"")") > 0
End Sub

```

<!-- repo-interconnect:start -->

---

## 🔗 Related Files
_Auto-generated by repo-interconnect · do not edit this section manually_

- [README.md](..\README.md) <!-- score: 1.200 | shared: function, how, vba, you -->
- [35   How do you sum with multiple OR criteria.md](..\Counting and Summing Functions\35   How do you sum with multiple OR criteria.md) <!-- score: 1.186 | shared: evaluate, function, how, you -->
- [70   How do you count unique values with criteria.md](..\Conditional Aggregation\70   How do you count unique values with criteria.md) <!-- score: 1.181 | shared: evaluate, how, uses, you -->
- [56   How do you remove duplicates with formulas.md](..\Advanced Formula Techniques\56   How do you remove duplicates with formulas.md) <!-- score: 1.175 | shared: function, how, uses, you -->
- [136   How do you create cross-tabulation  pivot-like  formulas.md](..\Advanced Array Operations\136   How do you create cross-tabulation  pivot-like  formulas.md) <!-- score: 1.167 | shared: function, how, uses, you -->

<!-- repo-interconnect:end -->
